package controllers.ti;

import repository.UserRepository;

public class TrustedIntermediaryControllerTest {
  UserRepository repo;

  /*  @Before
  public void setup()
  {
    repo = instanceOf(UserRepository.class);

  }
  @Test
  public void failTestOnMissingDateOfBirth()
  {
    AddApplicantToTrustedIntermediaryGroupForm form = AddApplicantToTrustedIntermediaryGroupForm();
    form.setFirstName("");
    repo.createNewApplicantForTrustedIntermediaryGroup(applicantform,tiGroup);
    Account applicant = repo.lookupAccountByEmail(EMAIL).get();
    assertThat(applicant.getApplicantName()).isEqualTo(applicantform.getFirstName()+applicantform.getLastName());
  }*/
}

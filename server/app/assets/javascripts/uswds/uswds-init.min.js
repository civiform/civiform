/* This file is copied directly from the USWDS package here:
node_modules/@uswds/uswds/dist/js/uswds-init.min.js. Its role is to help the
browser determine if the USWDS JavaScript bundle is loading properly. */

!(function () {
  'use strict'
  var n,
    e = 'usa-js-loading'
  function t() {
    document.documentElement.classList.remove(e)
  }
  document.documentElement.classList.add(e),
    (n = setTimeout(t, 8e3)),
    window.addEventListener(
      'load',
      function e() {
        window.uswdsPresent &&
          (clearTimeout(n), t(), window.removeEventListener('load', e, !0))
      },
      !0,
    )
})()
/* When Webpack creates the map file, it inserts the following comment at the
 end of the related minified JS file so that the browser knows where to find
 the source map. */
//# sourceMappingURL=uswds-init.min.js.map

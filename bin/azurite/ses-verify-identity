#! /usr/bin/env bash

# DOC: Verify SES email identity in azurite.

source bin/lib.sh
source bin/azurite/lib

verify_identity_cmd="verify-email-identity
  --region us-west-2
  --email-address ${1}"

azurite::run_command "${verify_identity_cmd}"

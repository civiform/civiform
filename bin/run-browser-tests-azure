#! /usr/bin/env python3

# DOC: Run a subset of browser tests that we want to specifically test in Azure in CI mode.

import subprocess

def get_test_files():
    # Disable Azure browser tests for now. We previously only ran file_upload.test.ts in
    # Azure (see https://github.com/civiform/civiform/issues/5299), but new file upload
    # tests aren't passing in Azure. Since there's no CEs planning to deploy with Azure,
    # it's not worth spending the time to try and fix the new file upload tests and
    # we'll instead just not run any tests on Azure.
    return []


shell_command = ["./bin/run-browser-tests-ci"] + get_test_files()
subprocess.run(shell_command, check=True)

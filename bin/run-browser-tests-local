#! /usr/bin/env bash

# DOC: Run the browser tests locally. Requires browser test env already running.

source bin/lib.sh

cd browser-test/ || exit
yarn install
BASE_URL=http://localhost:9999 DISABLE_SCREENSHOTS=true yarn test "$@"

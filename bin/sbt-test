#! /usr/bin/env bash

# DOC: Open an sbt CLI in the Unit Test environment container.

source bin/lib.sh
docker::set_project_name_dev

bin/pull-image

docker::compose_dev up -d --wait
docker::compose_dev exec -it civiform sbt -jvm-debug "*:8459" "$@" >.dockerlogs 2>&1 &
